package Week1.Labs.Lab1;
/*
public class RandomProblem {
    public int questionScore = 0;
    public int studentAnswer;
    private byte choiceMaker = (byte) (4 * Math.random());



    public int checkAnswer(){
        if (problem.getAnswer() == studentAnswer) {
            System.out.println("The answer is correct, keep going!");
            questionScore = (questionScore + 2);
        }
        else {
            System.out.println("The answer is wrong, you have one more try:");
            if (problem.getAnswer() == studentAnswer) {
                questionScore++;
            }
            else {
                System.out.println("The answer is still wrong, the correct answer is " + problem.getAnswer());
            }
        }
        return questionScore;
    }

}
*/